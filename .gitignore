node_modules
dist
dist-ssr
*.local

# Secrets — never commit. The backend reads its AudD token from here.
.env
server/.env
# Spotify OAuth tokens (refresh/access) — machine-local, never commit.
server/.spotify-tokens.json

# Set-list recap files are personal session logs, not source.
recaps/
*.tsbuildinfo
vite.config.js
vite.config.d.ts
.DS_Store
.vscode/*
!.vscode/extensions.json
